# SIM-B — Questions

**Navigation:** [Simulators index](../README.md) | [Result report](Relatorio.md)

- **Time:** 130 minutes
- **Language:** English
- **Rules:** Closed book. Complete all 65 questions before opening the answer key.

## Question metadata

| ID | Domain | Task | Format | Type | Difficulty |
|---|---:|---:|---|---|---|
| SIM-B-01 | 1 | 1.1 | single | fundamental | basic |
| SIM-B-02 | 1 | 1.1 | single | situational | intermediate |
| SIM-B-03 | 1 | 1.1 | single | integrated | advanced |
| SIM-B-04 | 1 | 1.1 | multi-2 | situational | intermediate |
| SIM-B-05 | 1 | 1.1 | single | integrated | advanced |
| SIM-B-06 | 1 | 1.1 | single | fundamental | basic |
| SIM-B-07 | 1 | 1.1 | single | situational | intermediate |
| SIM-B-08 | 1 | 1.2 | multi-2 | integrated | advanced |
| SIM-B-09 | 1 | 1.2 | single | situational | intermediate |
| SIM-B-10 | 1 | 1.2 | single | integrated | advanced |
| SIM-B-11 | 1 | 1.2 | single | fundamental | basic |
| SIM-B-12 | 1 | 1.2 | multi-2 | situational | intermediate |
| SIM-B-13 | 1 | 1.2 | single | integrated | advanced |
| SIM-B-14 | 1 | 1.2 | single | situational | intermediate |
| SIM-B-15 | 1 | 1.3 | single | integrated | advanced |
| SIM-B-16 | 1 | 1.3 | multi-2 | fundamental | basic |
| SIM-B-17 | 1 | 1.3 | single | situational | intermediate |
| SIM-B-18 | 1 | 1.3 | single | integrated | advanced |
| SIM-B-19 | 1 | 1.3 | single | situational | intermediate |
| SIM-B-20 | 1 | 1.3 | multi-2 | integrated | advanced |
| SIM-B-21 | 2 | 2.1 | single | fundamental | basic |
| SIM-B-22 | 2 | 2.1 | single | situational | intermediate |
| SIM-B-23 | 2 | 2.1 | single | integrated | advanced |
| SIM-B-24 | 2 | 2.1 | multi-2 | situational | intermediate |
| SIM-B-25 | 2 | 2.1 | single | integrated | advanced |
| SIM-B-26 | 2 | 2.1 | single | fundamental | basic |
| SIM-B-27 | 2 | 2.1 | single | situational | intermediate |
| SIM-B-28 | 2 | 2.1 | multi-2 | integrated | advanced |
| SIM-B-29 | 2 | 2.1 | single | situational | intermediate |
| SIM-B-30 | 2 | 2.2 | single | integrated | advanced |
| SIM-B-31 | 2 | 2.2 | single | fundamental | basic |
| SIM-B-32 | 2 | 2.2 | multi-2 | situational | intermediate |
| SIM-B-33 | 2 | 2.2 | single | integrated | advanced |
| SIM-B-34 | 2 | 2.2 | single | situational | intermediate |
| SIM-B-35 | 2 | 2.2 | single | integrated | advanced |
| SIM-B-36 | 2 | 2.2 | multi-2 | fundamental | basic |
| SIM-B-37 | 2 | 2.2 | single | situational | intermediate |
| SIM-B-38 | 3 | 3.1 | single | integrated | advanced |
| SIM-B-39 | 3 | 3.1 | single | situational | intermediate |
| SIM-B-40 | 3 | 3.1 | multi-2 | integrated | advanced |
| SIM-B-41 | 3 | 3.2 | single | fundamental | basic |
| SIM-B-42 | 3 | 3.2 | single | situational | intermediate |
| SIM-B-43 | 3 | 3.2 | single | integrated | advanced |
| SIM-B-44 | 3 | 3.3 | multi-2 | situational | intermediate |
| SIM-B-45 | 3 | 3.3 | single | integrated | advanced |
| SIM-B-46 | 3 | 3.3 | single | fundamental | basic |
| SIM-B-47 | 3 | 3.4 | single | situational | intermediate |
| SIM-B-48 | 3 | 3.4 | multi-2 | integrated | advanced |
| SIM-B-49 | 3 | 3.4 | single | situational | intermediate |
| SIM-B-50 | 3 | 3.5 | single | integrated | advanced |
| SIM-B-51 | 3 | 3.5 | single | fundamental | intermediate |
| SIM-B-52 | 3 | 3.5 | multi-3 | situational | intermediate |
| SIM-B-53 | 4 | 4.1 | single | integrated | intermediate |
| SIM-B-54 | 4 | 4.1 | single | situational | intermediate |
| SIM-B-55 | 4 | 4.1 | single | situational | intermediate |
| SIM-B-56 | 4 | 4.1 | multi-3 | fundamental | intermediate |
| SIM-B-57 | 4 | 4.2 | single | situational | intermediate |
| SIM-B-58 | 4 | 4.2 | single | integrated | intermediate |
| SIM-B-59 | 4 | 4.2 | single | situational | intermediate |
| SIM-B-60 | 4 | 4.3 | multi-3 | situational | intermediate |
| SIM-B-61 | 4 | 4.3 | single | fundamental | intermediate |
| SIM-B-62 | 4 | 4.3 | single | situational | intermediate |
| SIM-B-63 | 4 | 4.4 | single | integrated | intermediate |
| SIM-B-64 | 4 | 4.4 | multi-3 | situational | intermediate |
| SIM-B-65 | 4 | 4.4 | single | situational | intermediate |

## SIM-B-01

**Choose ONE.**

An enterprise has employees who need console access to several AWS accounts. The company already has a corporate identity provider and wants centralized sign-in with short-lived credentials instead of separate long-term IAM users in every account. Which service should be the foundation of the access design?

- A. Create one IAM user for every employee in every AWS account.
- B. Store shared administrator access keys in AWS Secrets Manager.
- C. Use AWS IAM Identity Center with the corporate identity provider and permission sets.
- D. Create Amazon Cognito user pools for the infrastructure administrators and map each pool separately to every AWS account.

## SIM-B-02

**Choose ONE.**

A consulting company operates a monitoring platform in its own AWS account. Hundreds of customers will allow that platform to read selected CloudWatch metrics in their accounts. The design must use temporary credentials and protect customers from the confused deputy problem. Which approach meets these requirements?

- A. Create a cross-account IAM role in each customer account and require a unique external ID when the consultant assumes it.
- B. Give the consulting company an access key created for the root user in each customer account.
- C. Add the consulting company's account to an AWS Organizations organizational unit in every customer environment and manage access through OU membership.
- D. Share each customer's CloudWatch service through AWS Resource Access Manager.

## SIM-B-03

**Choose ONE.**

A global company is migrating 180 applications into an AWS Organizations environment. Engineers authenticate through an external SAML identity provider. Platform teams need reusable job-function permissions, while production access must be narrower than development access and must be removed automatically when an employee leaves the directory. Which design provides the MOST operationally efficient governance?

- A. Create local IAM users in each account and periodically compare them with the external directory.
- B. Create one cross-account administrator role that every engineer can assume with a shared password.
- C. Federate the identity provider to IAM Identity Center and assign group-based permission sets to accounts by environment.
- D. Use Amazon Cognito identity pools to issue production administrator permissions to workforce users and synchronize a separate pool for every AWS account.

## SIM-B-04

**Choose TWO.**

A company needs a managed remote-access solution for individual engineers using laptops. Users must authenticate with the corporate SAML provider and different groups must reach different private VPC networks. The company does not want to operate VPN appliances. Which TWO configurations are required?

- A. Create an AWS Client VPN endpoint that uses federated authentication.
- B. Configure Client VPN routes and explicit authorization rules for the permitted destination networks.
- C. Create an AWS Site-to-Site VPN connection for every individual laptop.
- D. Attach an internet gateway directly to every private application subnet.
- E. Provision an AWS Direct Connect dedicated connection for each remote engineer.

## SIM-B-05

**Choose ONE.**

An organization has separate production accounts owned by application teams. Security requires an organization-wide guardrail that prevents disabling CloudTrail, even if a team administrator has AdministratorAccess. Teams must still receive their normal permissions through roles. Which design correctly separates the guardrail from granted permissions?

- A. Attach an SCP that denies the CloudTrail disable actions, and continue granting allowed actions through IAM roles.
- B. Attach an SCP that allows all approved actions and remove every IAM policy from the member accounts.
- C. Add a permissions boundary to the AWS account root user and use it as the only organization control.
- D. Create a resource share in AWS RAM that shares CloudTrail with all production accounts.

## SIM-B-06

**Choose ONE.**

An application running on Amazon EC2 must read objects from a private S3 bucket. The security team prohibits storing long-term access keys on instances. Which identity mechanism should the solutions architect use to provide automatically rotated temporary credentials?

- A. Embed an IAM user's access key in the EC2 user data.
- B. Attach an IAM role through an instance profile to the EC2 instance.
- C. Store the root user's credentials in an encrypted EBS volume.
- D. Create an Amazon Cognito user pool for the EC2 operating system and map each local user to a long-lived application identity.

## SIM-B-07

**Choose ONE.**

A regulated enterprise wants a break-glass process for the management account. Daily administration is performed through federated roles. The root user must be recoverable for the few tasks that require it, but exposure must be minimized. Which approach is MOST appropriate?

- A. Create root access keys and divide the key and secret between two administrators.
- B. Delete the root user after creating the first federated administrator role.
- C. Use the root user for daily console administration so that all services remain accessible.
- D. Protect the root user with MFA, avoid root access keys, secure recovery factors, and use it only for root-required tasks.

## SIM-B-08

**Choose TWO.**

A company is migrating into a 70-account AWS Organization. The security operations team needs managed threat detection enabled consistently and a central place to correlate and prioritize findings from multiple AWS security services. Which TWO actions best meet these requirements?

- A. Create identical standalone GuardDuty administrators manually in every member account.
- B. Send only application text logs to a single EC2 instance for manual review.
- C. Designate a delegated administrator for Amazon GuardDuty and enable organization-wide member management.
- D. Designate a delegated administrator for AWS Security Hub and configure centralized service management.
- E. Use AWS Artifact to aggregate GuardDuty and Inspector findings.

## SIM-B-09

**Choose ONE.**

A public application is behind an Application Load Balancer. Recent requests contain SQL injection and cross-site scripting patterns. The company wants a managed control that can inspect HTTP requests and block these patterns before they reach the targets. Which service should be added?

- A. A network ACL that denies the database TCP port.
- B. AWS WAF associated with the Application Load Balancer.
- C. Amazon GuardDuty malware protection for the target group.
- D. AWS Shield Standard configured with SQL statements.

## SIM-B-10

**Choose ONE.**

A financial company migrates administrative servers into private subnets that have no NAT gateway and no inbound route from the internet. Operators require audited shell access and Patch Manager must reach Systems Manager without traversing the public internet. Which design meets the requirement with the LEAST operational overhead?

- A. Deploy a bastion host with a public IP and open SSH from every operator address.
- B. Attach an internet gateway to the private route table and store SSH keys in Parameter Store.
- C. Use Systems Manager managed nodes with an instance role and the required Systems Manager interface VPC endpoints.
- D. Create a Site-to-Site VPN only, without installing or configuring Systems Manager prerequisites.

## SIM-B-11

**Choose ONE.**

A solutions architect is reviewing the network controls for a three-tier VPC. The architect needs a stateful virtual firewall that can allow traffic from the application tier to the database tier by referencing the application tier control rather than individual IP addresses. Which control provides this behavior?

- A. An internet gateway rule that permits the application subnet.
- B. A network ACL deny rule that references another network ACL.
- C. A route table entry that targets the application security group.
- D. A security group that references the application security group as its source.

## SIM-B-12

**Choose TWO.**

An Amazon EKS application needs a database password at runtime. Only one Kubernetes service account should be able to retrieve the password, and the operations team wants managed secret rotation without placing the secret in a container image or ConfigMap. Which TWO actions satisfy the requirements?

- A. Associate a least-privilege IAM role with the workload service account by using EKS Pod Identity or an equivalent service-account role mechanism.
- B. Grant Secrets Manager wildcard access to the IAM role used by every worker node.
- C. Place the plaintext password in a Kubernetes ConfigMap committed to the deployment repository.
- D. Bake a long-lived IAM access key and the database password into the container image.
- E. Store and rotate the password in AWS Secrets Manager and mount or retrieve it through a supported workload integration.

## SIM-B-13

**Choose ONE.**

An enterprise connects dozens of spoke VPCs and two data centers through AWS Transit Gateway. Security requires centralized stateful inspection of east-west and hybrid traffic. Return traffic must traverse the same firewall endpoints so that stateful flows are not broken. Which architecture best meets the requirement?

- A. Associate AWS WAF with Transit Gateway and inspect every TCP packet in a web ACL.
- B. Deploy AWS Network Firewall in a dedicated inspection VPC and configure Transit Gateway routing for symmetric appliance traversal.
- C. Enable GuardDuty in the network account and use its findings as inline packet deny rules.
- D. Create one stateless network ACL in any spoke VPC and share it through AWS RAM.

## SIM-B-14

**Choose ONE.**

A revenue-critical public application already receives the automatic network and transport layer DDoS protection included with AWS services. The company now requires enhanced DDoS detection, access to the AWS DDoS Response Team, and DDoS cost protection for eligible scaling charges. Which service should be selected?

- A. AWS Shield Standard with a custom SQL injection rule.
- B. Amazon GuardDuty with Runtime Monitoring enabled.
- C. AWS Firewall Manager without enabling a protection service, using one centrally managed policy across every account.
- D. AWS Shield Advanced for the protected application resources.

## SIM-B-15

**Choose ONE.**

A central security account owns a customer managed KMS key. A workload role in a separate production account must decrypt a shared encrypted dataset. The role already has an IAM allow policy for kms:Decrypt, but requests are denied. Which change completes a least-privilege cross-account design?

- A. Make the encrypted S3 objects public so that the KMS authorization step is skipped.
- B. Add the production role or its account to the KMS key policy in the security account and retain the role's IAM permission.
- C. Attach an SCP with an Allow statement for kms:Decrypt to the production organizational unit.
- D. Create an alias with the same name in the production account without changing the original key policy.

## SIM-B-16

**Choose TWO.**

A new S3 bucket will store confidential migration files. Company policy requires TLS for every request and encryption at rest with a specified customer managed KMS key. Which TWO controls directly enforce these requirements?

- A. Enable an S3 website endpoint and require clients to use HTTP.
- B. Add a bucket policy that denies requests when aws:SecureTransport is false.
- C. Configure default SSE-KMS encryption with the required customer managed key and restrict unauthorized encryption headers as needed.
- D. Use an S3 ACL that grants Everyone read access to encrypted objects.
- E. Rely only on the physical security of the AWS data center.

## SIM-B-17

**Choose ONE.**

A migrated application connects to Amazon RDS by using a database password. The password must be encrypted, retrieved at runtime under IAM control, and rotated automatically without administrators maintaining a custom rotation scheduler. Which service best meets the requirement?

- A. AWS Secrets Manager with an appropriate RDS rotation configuration.
- B. A plaintext CloudFormation parameter stored in the template.
- C. An EC2 user data script containing the permanent password.
- D. Amazon S3 Transfer Acceleration with server-side encryption.

## SIM-B-18

**Choose ONE.**

During an acquisition, an enterprise receives hundreds of S3 buckets distributed across multiple AWS accounts. Compliance needs automated discovery of personally identifiable information, risk findings, and centralized administration without copying every object into a security account. Which design is MOST appropriate?

- A. Use Amazon Macie with an organization delegated administrator and configure classification jobs for in-scope buckets.
- B. Use Amazon Inspector to scan every S3 object for software package vulnerabilities.
- C. Use AWS Artifact to download a separate PII inventory for each member account.
- D. Use Amazon Detective to rewrite every object with a classification tag.

## SIM-B-19

**Choose ONE.**

A regulated company protects backups from ransomware. Once a backup is stored in a designated vault, no user, including an administrator in the account, may delete it or shorten its retention period before expiration. The control must become immutable after a grace period. Which feature should be used?

- A. An AWS Budgets action for the backup vault.
- B. AWS Backup Vault Lock in compliance mode.
- C. An S3 lifecycle rule that immediately expires every backup.
- D. A security group that denies HTTPS access to AWS Backup.

## SIM-B-20

**Choose TWO.**

A manufacturer will use AWS DataSync to migrate confidential NFS files from a plant to Amazon S3. Security requires encryption while data crosses the network and encryption at rest under a customer managed KMS key. Which TWO controls satisfy the requirements?

- A. Expose the NFS share directly to the public internet over an unencrypted protocol.
- B. Use an S3 public-read ACL because encrypted objects are safe to expose.
- C. Disable KMS key rotation and grant kms:* to every principal in the organization.
- D. Use DataSync's encrypted service communication and transfer channel for data in transit.
- E. Configure the destination bucket for SSE-KMS with the approved key and grant the DataSync role the required KMS permissions.

## SIM-B-21

**Choose ONE.**

An order API receives traffic faster than a backend worker can process it during short peaks. Orders must be durably buffered so that the API and worker can scale independently and temporary worker failures do not lose requests. Which service is the primary decoupling component?

- A. AWS Direct Connect gateway.
- B. Amazon Route 53 Resolver outbound endpoint.
- C. Amazon Simple Queue Service (Amazon SQS).
- D. Amazon EC2 Dedicated Host.

## SIM-B-22

**Choose ONE.**

When a shipment status changes, billing, analytics, and customer-notification systems must each receive a copy of the event. A failure or backlog in one consumer must not delay the others, and each consumer needs its own retry behavior. Which architecture should be used?

- A. Place all consumers behind one Application Load Balancer target group.
- B. Publish to an Amazon SNS topic with a separate Amazon SQS subscription for each consumer.
- C. Write the event to one EC2 instance and let consumers poll its local disk.
- D. Create a single SQS queue from which all three systems compete for each message.

## SIM-B-23

**Choose ONE.**

A retailer is decomposing an on-premises order system. Events for the same order must be processed in sequence, duplicate submissions within the deduplication interval should not create duplicate work, and different orders should process concurrently. Which queue configuration best supports the migration?

- A. An Amazon SQS FIFO queue using the order ID as the message group ID and stable deduplication IDs.
- B. An Amazon SQS standard queue with a visibility timeout of zero seconds.
- C. An Amazon SNS standard topic with no durable subscriber.
- D. A single-instance RabbitMQ broker with no persistent storage.

## SIM-B-24

**Choose TWO.**

A stateless web application is being migrated from one server to Amazon EC2. It must distribute HTTP requests across healthy instances in multiple Availability Zones and automatically add or remove instances as demand changes. Which TWO services form the core design?

- A. An Application Load Balancer configured across the selected Availability Zones.
- B. One fixed-size EC2 instance with an Elastic IP address.
- C. An EC2 Auto Scaling group spanning multiple Availability Zones.
- D. An AWS Storage Gateway tape pool for HTTP session routing.
- E. A single NAT instance used as the public application endpoint.

## SIM-B-25

**Choose ONE.**

A company migrates a synchronous web tier to AWS Lambda but keeps its relational database on Amazon Aurora. During traffic bursts, thousands of concurrent functions open database connections and exhaust the database connection limit. The company wants pooling and faster recovery during database failover with minimal code change. What should be added?

- A. S3 Transfer Acceleration for the database endpoint.
- B. An internet gateway between Lambda and the database subnet.
- C. A larger Amazon SQS visibility timeout for every SQL connection.
- D. Amazon RDS Proxy between the Lambda functions and the Aurora database.

## SIM-B-26

**Choose ONE.**

A web tier stores each signed-in user's session only on the local disk of the EC2 instance that handled the login. The company wants to add and remove instances freely behind a load balancer without losing sessions. Which architectural change best supports horizontal scaling?

- A. Increase the size of the local instance store on every web server.
- B. Store session state in a shared service such as DynamoDB or ElastiCache.
- C. Assign a different public IP address to every user session.
- D. Disable health checks so that instances are never removed.

## SIM-B-27

**Choose ONE.**

Business units publish application events from separate AWS accounts. A central platform must route events to consumers according to event content, and producers must not know consumer endpoints. The company wants a managed service with cross-account event-bus policies. Which service should be used?

- A. Amazon EventBridge with custom event buses, rules, and resource-based policies.
- B. AWS Direct Connect with a public virtual interface.
- C. Amazon EBS Multi-Attach across all of the accounts.
- D. AWS Certificate Manager Private Certificate Authority.

## SIM-B-28

**Choose TWO.**

A migration pipeline receives millions of documents in S3 during irregular bursts. OCR workers can process only a limited rate. Every document must be retried after transient failures, and repeatedly failing work must be isolated for investigation without stopping successful documents. Which TWO design elements should be used?

- A. Invoke every worker synchronously from the upload client and retry forever in the client.
- B. Buffer document work in an Amazon SQS queue consumed at a controlled concurrency.
- C. Store pending document identifiers only in an EC2 instance store volume.
- D. Configure an SQS dead-letter queue with a suitable maximum receive count.
- E. Disable the SQS visibility timeout so every worker receives the same message immediately.

## SIM-B-29

**Choose ONE.**

A partner submits archive-conversion jobs through an HTTPS API. Each job can take 25 minutes, and the partner must receive an immediate acknowledgement instead of holding an HTTP connection open. Workers should scale based on queued work. Which architecture is MOST appropriate?

- A. Run the conversion inside a Route 53 health check and return its health status.
- B. Keep the API Gateway request open until one Lambda function finishes all 25 minutes of processing before returning an HTTP response.
- C. Have API Gateway enqueue the job in SQS, return a job ID, and let scalable workers update job status.
- D. Write each job into an ALB cookie and ask the partner to preserve the cookie.

## SIM-B-30

**Choose ONE.**

A payment application uses an Aurora PostgreSQL database. A regional disaster must cause no more than a few seconds of data loss, and the database must become writable in another Region within minutes. Read traffic may use the secondary Region before a disaster. Which database design best meets these objectives?

- A. Export database logs to CloudWatch Logs and recreate records manually after a disaster.
- B. Take one manual snapshot each month and copy it to the secondary Region.
- C. Use an in-Region Aurora Replica without any resources in another Region.
- D. Use an Aurora global database and define a tested cross-Region switchover or failover runbook.

## SIM-B-31

**Choose ONE.**

A business owner says that after a disaster the application can be unavailable for four hours and can lose at most fifteen minutes of committed data. Which statement correctly identifies the recovery objectives?

- A. The RTO is four hours and the RPO is fifteen minutes.
- B. The RPO is four hours and the RTO is fifteen minutes.
- C. Both the RTO and RPO are four hours.
- D. Both the RTO and RPO are fifteen minutes.

## SIM-B-32

**Choose TWO.**

A company relies on AWS Backup for production resources. Its resilience policy requires recovery points outside the production account and evidence that selected resources can actually be restored. Which TWO actions address these requirements?

- A. Keep every recovery point only in the same production vault and same Region.
- B. Assume that a successful backup job proves the application can be recovered.
- C. Configure backup copy actions to an appropriately secured cross-account or cross-Region vault.
- D. Delete old recovery points before testing so that only one restore option remains.
- E. Use AWS Backup restore testing plans for supported resources and review the validation results.

## SIM-B-33

**Choose ONE.**

A manufacturer has hundreds of VMware and physical servers that cannot be redesigned before a data-center exit. It needs continuous block-level replication into a low-cost staging area in AWS and automated launch of recovery instances during drills or a disaster. Which service is designed for this requirement?

- A. Amazon AppFlow.
- B. AWS Database Migration Service.
- C. AWS Elastic Disaster Recovery.
- D. AWS Transfer Family.

## SIM-B-34

**Choose ONE.**

A media archive stores new objects in an S3 bucket in one Region. Compliance requires automatic asynchronous copies in a second Region under a separate destination bucket. Existing application writes should continue using the source bucket. Which configuration meets the requirement?

- A. Enable versioning on both buckets and configure S3 Cross-Region Replication with an authorized replication role.
- B. Create an EBS snapshot lifecycle policy for the source bucket.
- C. Attach both S3 buckets to the same security group.
- D. Use an in-Region S3 gateway endpoint without a replication rule.

## SIM-B-35

**Choose ONE.**

An enterprise uses one AWS Direct Connect connection for latency-sensitive hybrid applications. A physical circuit failure must not isolate the data center, but the company accepts lower performance on the emergency path. Routing should fail over dynamically with minimal manual intervention. Which design is MOST resilient?

- A. Create a larger Direct Connect VLAN on the same failed circuit.
- B. Add a second virtual interface on the same physical Direct Connect connection and no other path.
- C. Rely on an internet gateway in an AWS VPC to terminate data-center routes.
- D. Add a Site-to-Site VPN over the internet as a backup and use BGP routing preferences with health-tested tunnels.

## SIM-B-36

**Choose TWO.**

A team is choosing between an Amazon RDS Multi-AZ deployment and read replicas for a relational database. Which TWO statements correctly describe their primary architectural purposes?

- A. A Multi-AZ DB instance maintains a synchronous standby and supports automatic failover.
- B. A Multi-AZ standby is the preferred endpoint for scaling application read traffic.
- C. Read replicas always use synchronous replication and always fail over automatically with no application planning.
- D. Read replicas can serve read traffic, while the classic Multi-AZ standby is primarily for availability.
- E. Neither feature creates an additional database copy.

## SIM-B-37

**Choose ONE.**

A globally distributed shopping-cart service uses DynamoDB. Users must read and write through a nearby Region, and the application must continue operating in another Region if one Region becomes unavailable. The team wants managed multi-Region replication. Which feature should be used?

- A. A single DynamoDB table with one gateway endpoint and no regional replica.
- B. A DynamoDB local secondary index in only the primary Region.
- C. DynamoDB global tables with application routing to healthy regional endpoints.
- D. Nightly DynamoDB exports as the active request path.

## SIM-B-38

**Choose ONE.**

A company is migrating a Windows engineering file share to AWS. Applications require SMB, Microsoft Active Directory integration, Windows ACLs, and a highly available managed file system across Availability Zones. Users must continue accessing files through standard Windows semantics. Which storage service should be selected?

- A. Amazon EFS mounted by using the SMB protocol.
- B. Amazon FSx for Windows File Server using a Multi-AZ deployment.
- C. Amazon S3 exposed as a native Windows NTFS volume with security groups.
- D. An EC2 instance store volume shared directly across Availability Zones.

## SIM-B-39

**Choose ONE.**

Field teams around the world upload multi-gigabyte objects to one S3 bucket in a Region far from many users. Network paths over the public internet are inconsistent. The company wants to use the AWS edge network to accelerate long-distance uploads without deploying regional application servers. Which feature should be enabled?

- A. A DynamoDB Accelerator cluster in each field office.
- B. S3 Glacier Deep Archive expedited retrieval.
- C. An EBS Provisioned IOPS volume attached to the bucket.
- D. Amazon S3 Transfer Acceleration.

## SIM-B-40

**Choose TWO.**

A research organization is moving an HPC pipeline to AWS. Source datasets are durable in S3, but thousands of compute cores need a shared POSIX file system with very high throughput during each run. The processing file system may be deleted after verified results are returned to S3. Which TWO actions provide the best fit?

- A. Have every compute process modify the same S3 object as if it were a POSIX file.
- B. Create an Amazon FSx for Lustre file system with an S3 data repository association.
- C. Attach one EBS volume simultaneously to instances in different Availability Zones.
- D. Use an Amazon EFS One Zone file system as an object-storage replacement without evaluating throughput.
- E. Mount FSx for Lustre on the compute fleet and export completed results to S3 before cleanup.

## SIM-B-41

**Choose ONE.**

A containerized scientific job runs for approximately 40 minutes, needs 8 vCPUs, and starts only when input files arrive. The company wants managed job queues and compute environments. Which compute service is a better fit than a standard AWS Lambda function?

- A. Run the job by using the AWS Batch managed service.
- B. Amazon Route 53.
- C. AWS Certificate Manager.
- D. Amazon Cognito.

## SIM-B-42

**Choose ONE.**

A company has already containerized a stateless API. Traffic is highly variable, and the operations team does not want to patch or manage EC2 worker nodes. The API must scale by running more container tasks. Which compute option has the lowest infrastructure-management burden?

- A. Run one permanent container on an EC2 Dedicated Host.
- B. Run the containers as Amazon ECS tasks on AWS Fargate.
- C. Install Docker manually on a single on-premises server.
- D. Store the container image in S3 and invoke it as a Route 53 record.

## SIM-B-43

**Choose ONE.**

A factory control workload must run inside the company's facility because it requires single-digit-millisecond access to production equipment and local processing during a temporary WAN disruption. The company wants AWS-managed infrastructure, familiar AWS APIs, and integration with its parent Region. Which solution is MOST appropriate?

- A. Deploy the workload on AWS Outposts installed at the factory.
- B. Move the controllers to a public S3 bucket in the nearest Region.
- C. Use Amazon CloudFront as the operating system for factory servers.
- D. Use S3 Cross-Region Replication as the real-time compute platform.

## SIM-B-44

**Choose TWO.**

An ecommerce application uses Amazon RDS for MySQL. Product-catalog reads create 90% of database traffic, many requests repeat the same popular queries, and writes must continue going to the primary database. Which TWO changes can improve read performance while preserving the relational system of record?

- A. Create RDS read replicas and direct appropriate read-only queries to them.
- B. Cache frequently requested catalog data in Amazon ElastiCache with a defined invalidation strategy.
- C. Send every read and write to the same primary instance with no scaling change.
- D. Replace the database endpoint with an internet gateway.
- E. Store MySQL transaction logs only in an EC2 instance store volume.

## SIM-B-45

**Choose ONE.**

A fraud-investigation platform must traverse billions of relationships among accounts, devices, IP addresses, and transactions. Queries frequently ask for multi-hop paths and changing connections. The company wants a managed database purpose-built for highly connected data rather than implementing joins in a relational schema. Which service should be used?

- A. Amazon Timestream using only time-window aggregates.
- B. Amazon ElastiCache for Memcached as the permanent graph system of record.
- C. Amazon Neptune.
- D. Amazon RDS for SQL Server with every relationship stored in one text column.

## SIM-B-46

**Choose ONE.**

A DynamoDB table will receive a very high request rate. The architect can choose the partition key. Which characteristic generally provides the best opportunity for DynamoDB to distribute traffic evenly across its underlying partitions?

- A. A high-cardinality key whose request values are distributed across many distinct items.
- B. A constant key value used by every write in the application.
- C. A key that always contains the current month for all customers.
- D. A Boolean key with only true and false as possible values.

## SIM-B-47

**Choose ONE.**

A software provider exposes an application from a service VPC. Consumer VPCs in several customer accounts must reach the service through private IP addresses without internet gateways, NAT gateways, or a full mesh of VPC peering connections. Which networking design best meets the requirement?

- A. Create an S3 gateway endpoint and use it for the provider's custom TCP service.
- B. Create transitive VPC peering among every provider and consumer VPC.
- C. Assign a public IPv4 address to every service target and consumer instance.
- D. Publish a VPC endpoint service behind a Network Load Balancer and let consumers create interface VPC endpoints through AWS PrivateLink.

## SIM-B-48

**Choose TWO.**

A bank is migrating latency-sensitive services but will retain its mainframe on premises. It requires a dedicated network path with predictable performance to AWS and also requires IPsec encryption for application traffic on that path. Which TWO connectivity services should be combined?

- A. Amazon CloudFront.
- B. AWS Client VPN for each mainframe interface.
- C. AWS Direct Connect.
- D. AWS Site-to-Site VPN configured over the supported Direct Connect design.
- E. Amazon S3 Transfer Acceleration.

## SIM-B-49

**Choose ONE.**

A gaming service exposes TCP endpoints in two AWS Regions. Clients include devices that cache IP addresses, so the company needs static anycast IP addresses, health-based routing to healthy regional endpoints, and improved performance over the AWS global network. Which service best fits?

- A. A Route 53 private hosted zone only.
- B. Amazon CloudFront signed cookies.
- C. AWS Global Accelerator.
- D. An S3 gateway endpoint.

## SIM-B-50

**Choose ONE.**

A company is migrating a 12 TB Oracle database to Amazon Aurora PostgreSQL. The source must remain online during most of the migration, changes must continue replicating until cutover, and incompatible schema objects must be converted and tested. Which approach is MOST appropriate?

- A. Use Route 53 weighted records to replicate database rows between engines.
- B. Use AWS DataSync alone to copy Oracle data files directly into the Aurora storage volume.
- C. Use an S3 lifecycle rule to transform Oracle procedures into PostgreSQL functions.
- D. Use AWS Schema Conversion Tool for schema assessment and conversion, then AWS DMS full load plus CDC for the data.

## SIM-B-51

**Choose ONE.**

An enterprise must perform recurring, incremental online transfers from an on-premises NFS file system to Amazon S3. It needs scheduling, bandwidth controls, metadata handling, and end-to-end data verification. Which managed transfer service is the best fit?

- A. Amazon Kinesis Data Streams.
- B. AWS Storage Gateway Volume Gateway.
- C. AWS DataSync.
- D. AWS Database Migration Service.

## SIM-B-52

**Select THREE.**

A digital platform produces a high-volume stream of click events. It needs ordered stream ingestion with retained records for replay, stateful windowed transformations, and continuous buffered delivery of transformed output to S3 in a compressed analytics format. Which THREE services form an appropriate managed pipeline?

- A. Amazon Kinesis Data Streams for durable streaming ingestion and replay.
- B. Amazon Managed Service for Apache Flink for stateful windowed stream processing.
- C. Amazon Data Firehose for buffered delivery and format conversion to Amazon S3.
- D. Amazon SQS FIFO as the only stateful streaming analytics engine.
- E. Amazon RDS Proxy for event serialization.
- F. AWS Database Migration Service as the public click-collection endpoint.

## SIM-B-53

**Choose ONE.**

After a data-center migration, an enterprise will store hundreds of millions of long-lived objects in S3. Access patterns are unpredictable and can change over time. Objects must remain available with millisecond access, and the company wants automatic movement among access tiers without writing lifecycle predictions for each dataset. Which storage class is the best fit?

- A. S3 Glacier Deep Archive for every object immediately after upload, submitting a restore request whenever access is needed.
- B. S3 Intelligent-Tiering using its automatic millisecond-access tiers.
- C. S3 One Zone-IA for the only copy of every irreplaceable object.
- D. Amazon EBS io2 volumes attached permanently to one archive server.

## SIM-B-54

**Choose ONE.**

A fleet of general-purpose EC2 instances uses gp2 EBS volumes. Most volumes are large only because the applications need more baseline IOPS, not more capacity. The team wants to provision required size, IOPS, and throughput independently and reduce storage cost without changing to a specialized high-endurance workload. Which volume type should be evaluated?

- A. S3 Glacier Flexible Retrieval mounted as an EC2 boot volume.
- B. EBS Provisioned IOPS SSD io2 Block Express for every volume.
- C. EC2 instance store as the only durable database storage.
- D. EBS General Purpose SSD gp3.

## SIM-B-55

**Choose ONE.**

A shared Amazon EFS file system contains project directories that are active for several weeks and then are rarely opened. Applications must retain the same NFS namespace and should not be rewritten. Which configuration reduces storage cost with the least operational effort?

- A. Increase provisioned throughput so inactive files are read faster.
- B. Copy every inactive file manually to an EC2 instance store volume and maintain a redirect catalog for every application.
- C. Configure EFS lifecycle management to transition inactive files to an EFS IA storage class.
- D. Create one EBS volume and mount it concurrently from instances in every Region.

## SIM-B-56

**Select THREE.**

A solutions architect is comparing AWS storage classes for cost optimization. Which THREE statements correctly describe important cost or availability characteristics that should influence the choice?

- A. S3 Standard-IA has retrieval charges and a minimum storage-duration charge.
- B. S3 Intelligent-Tiering requires the architect to predict and manually move every object between its automatic tiers.
- C. Every S3 object can transition immediately to any class without minimum-size or minimum-duration considerations.
- D. S3 One Zone-IA stores data in a single Availability Zone and is best for re-creatable or secondary copies.
- E. An EBS snapshot must remain attached to a running EC2 instance to preserve its data.
- F. S3 Glacier Flexible Retrieval and Deep Archive generally require a restore process before archived data is accessed.

## SIM-B-57

**Choose ONE.**

An enterprise has a predictable baseline of compute spending for the next three years, but teams may change EC2 instance families, Regions, and the balance among EC2, Fargate, and Lambda. Which purchasing option provides a discount while preserving the greatest flexibility across these compute services?

- A. Dedicated Hosts purchased independently for every Lambda function.
- B. EC2 Instance Savings Plans tied to one instance family and Region.
- C. Spot Instances as a guarantee that all baseline production capacity will always be available.
- D. A Compute Savings Plan with an appropriate hourly commitment.

## SIM-B-58

**Choose ONE.**

A media company migrates a fault-tolerant rendering farm to EC2. Jobs are checkpointed and can resume after interruption. A small amount of capacity must always be available, but burst capacity should use the lowest-cost compatible instances across several families. Which design is MOST cost-effective?

- A. Run the entire peak fleet as fixed On-Demand instances continuously.
- B. Use an EC2 Auto Scaling mixed instances policy with an On-Demand base and diversified Spot capacity for bursts.
- C. Purchase Dedicated Hosts for every possible peak instance and leave them idle.
- D. Use one very large EC2 instance with no checkpoint recovery or queue.

## SIM-B-59

**Choose ONE.**

A platform team suspects that many EC2 instances and ECS services are overprovisioned, but it must preserve performance. It wants recommendations derived from recent configuration and utilization metrics, including projected utilization for candidate sizes. Which service should the team use first?

- A. AWS Compute Optimizer.
- B. AWS Artifact.
- C. Amazon Route 53 Resolver.
- D. AWS CloudHSM.

## SIM-B-60

**Select THREE.**

A steady Amazon RDS for MySQL production database must remain Multi-AZ. Monitoring shows sustained CPU below 20%, the application is compatible with AWS Graviton database classes, and the company expects to run the database for three more years. Which THREE actions can reduce cost without removing the availability requirement?

- A. Add multiple unused read replicas even though no read-scaling requirement exists.
- B. Right-size the DB instance after validating memory, connections, latency, and peak utilization.
- C. Evaluate an eligible Graviton-based DB instance class and performance-test the application.
- D. Disable Multi-AZ even though the stated production availability requirement remains.
- E. Purchase an appropriate RDS Reserved DB Instance commitment for the stable baseline.
- F. Replace the relational database immediately with DynamoDB without assessing schema or access patterns.

## SIM-B-61

**Choose ONE.**

A new DynamoDB workload has unknown traffic levels and can experience sudden unpredictable peaks. The team wants to avoid capacity planning and pay for the requests that the application performs. Which capacity mode is the most appropriate initial choice?

- A. An RDS Reserved DB Instance applied to the DynamoDB table.
- B. Provisioned capacity fixed permanently at one read and one write unit.
- C. DynamoDB on-demand capacity mode.
- D. EC2 Spot capacity assigned as DynamoDB partitions.

## SIM-B-62

**Choose ONE.**

An Aurora PostgreSQL cluster is highly I/O intensive. Monthly analysis shows that I/O charges are a large and consistent portion of total database spend. The workload will continue at this level, and the team wants predictable pricing without paying per I/O operation. Which storage configuration should be evaluated?

- A. Aurora Standard with additional unnecessary read replicas.
- B. Aurora I/O-Optimized cluster storage configuration.
- C. S3 Glacier Deep Archive as the live Aurora transaction volume.
- D. An EC2 instance store volume manually attached to the Aurora writer.

## SIM-B-63

**Choose ONE.**

After migrating an analytics fleet into private subnets, a company discovers that most NAT gateway data processing is caused by large transfers from EC2 to S3 in the same Region. The instances do not need the NAT path for S3. Which change will reduce network cost while preserving private connectivity?

- A. Assign public IPv4 addresses to every analytics instance.
- B. Move the S3 traffic through an additional public NAT gateway.
- C. Send the S3 traffic through a Transit Gateway in another Region.
- D. Create an S3 gateway endpoint and associate it with the private subnet route tables.

## SIM-B-64

**Select THREE.**

A network cost review finds three major patterns: private instances reach S3 through NAT gateways, global users repeatedly download the same cacheable objects from regional origins, and application tiers make unnecessary cross-Availability Zone calls to cache nodes. Which THREE changes directly address these cost drivers?

- A. Centralize all outbound traffic through one distant NAT gateway regardless of Availability Zone or resilience.
- B. Provision a Direct Connect circuit for every individual internet user.
- C. Route same-Region traffic through another AWS Region before reaching its destination.
- D. Add S3 gateway endpoints and route eligible S3 traffic through them.
- E. Use Amazon CloudFront to cache and deliver the repeatedly requested public objects.
- F. Place cache capacity in the required Availability Zones and route clients to local cache nodes when the service supports it.

## SIM-B-65

**Choose ONE.**

A company will exchange several terabytes of data every day between its primary data center and AWS for at least five years. The transfer rate is steady, predictable network performance is important, and the organization can complete carrier provisioning. Which connectivity option should be evaluated for long-term price and performance?

- A. An appropriately sized AWS Direct Connect connection.
- B. A separate AWS Client VPN endpoint for every server.
- C. S3 Transfer Acceleration as the private routing protocol for all applications.
- D. A public IPv4 address on every on-premises storage device.
